rootProject.name = 'sciorder'

if (HEROKU == "true") SKIP_ANDROID="true"

include ':common'
include ':server'
include ':client'
include ':client-js'

if(SKIP_ANDROID != "true") {
    include ':client-android'
    include ':client-android-user'
    include ':client-android-admin'
}
